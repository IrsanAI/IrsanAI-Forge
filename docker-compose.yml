services:
  forge:
    build:
      context: .
      dockerfile: Dockerfile
    image: irsanai-forge:latest
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
